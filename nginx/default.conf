upstream api {
  server api:3000;
}

upstream fe {
  server fe:5000;
}

server {
  listen 80;

  location / {
    proxy_pass http://fe;
  }

  location /api {
    rewrite /api/(.*) /$1 break;
    proxy_pass http://api;
  }
}